/** Alineación del menú respecto al trigger. */
export type WiMenuAlign = 'start' | 'center' | 'end';

/** Lado preferido del menú respecto al trigger. */
export type WiMenuSide = 'top' | 'bottom' | 'left' | 'right';

/** Variante visual de un ítem de menú. */
export type WiMenuItemVariant = 'default' | 'danger';
