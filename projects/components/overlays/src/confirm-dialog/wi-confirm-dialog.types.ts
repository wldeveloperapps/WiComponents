export type WiConfirmDialogSize = 'sm' | 'md';

export type WiConfirmDialogState = 'open' | 'closed';

/** Variantes del botón de confirmación (subset de `WiButtonVariant`). */
export type WiConfirmDialogConfirmVariant = 'primary' | 'danger';
