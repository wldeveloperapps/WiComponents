export type WiConfirmPopupSize = 'sm' | 'md';

export type WiConfirmPopupState = 'open' | 'closed';

/** Alineación horizontal del popup respecto al trigger. */
export type WiConfirmPopupAlign = 'start' | 'center' | 'end';

/** Variantes del botón de confirmación (subset de `WiButtonVariant`). */
export type WiConfirmPopupConfirmVariant = 'primary' | 'danger';
