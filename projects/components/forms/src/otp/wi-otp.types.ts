export type WiOtpSize = 'sm' | 'md' | 'lg';

/** Teclado virtual del input nativo. `numeric` es el caso de login (solo dígitos). */
export type WiOtpInputMode = 'numeric' | 'text' | 'tel';

export type WiOtpAutocomplete = 'one-time-code' | 'off';
