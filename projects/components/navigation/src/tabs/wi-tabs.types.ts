export type WiTabsOrientation = 'horizontal' | 'vertical';

export type WiTabsActivationMode = 'automatic' | 'manual';

/** `segmented` = select-button / píldoras (panel de alertas); `line` = subrayado. */
export type WiTabsListVariant = 'segmented' | 'line';

export type WiTabsSize = 'sm' | 'md';
