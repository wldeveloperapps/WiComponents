/**
 * Clase CSS que la aplicación consumidora aplica al documento (recomendado: `<html>`)
 * para activar el tema oscuro. La librería no muta el DOM.
 */
export const WI_DARK_CLASS = 'wi-dark' as const;

/** Prefijo público de variables CSS de color. */
export const WI_COLOR_TOKEN_PREFIX = '--wi-color-' as const;
